#!/bin/bash
set -euo pipefail
case "${1:-}" in
probe)   command -v dnf >/dev/null ;;
install) shift; exec sudo dnf install -y -- "$@" ;;
*) echo "usage: dnf probe|install <pkg>..." >&2; exit 1 ;;
esac
