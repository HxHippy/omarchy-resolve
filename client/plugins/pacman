#!/bin/bash
# Arch's native package manager. The default route on Omarchy.
set -euo pipefail
case "${1:-}" in
probe)   command -v pacman >/dev/null ;;
install) shift; exec sudo pacman -S --needed --noconfirm -- "$@" ;;
*) echo "usage: pacman probe|install <pkg>..." >&2; exit 1 ;;
esac
