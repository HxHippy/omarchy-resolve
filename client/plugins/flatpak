#!/bin/bash
# Cross-distro app delivery. Sandboxed, so it coexists with a native install.
set -euo pipefail
case "${1:-}" in
probe)   command -v flatpak >/dev/null ;;
install) shift; exec flatpak install -y flathub "$@" ;;
*) echo "usage: flatpak probe|install <app-id>..." >&2; exit 1 ;;
esac
